/**
 * Re-export of the webhook delivery queue wire types. Implementation lives in
 * `@releases/core-internal/webhook-delivery` (shared with the webhooks worker).
 */
export type { DeliveryMessage } from "@releases/core-internal/webhook-delivery";
