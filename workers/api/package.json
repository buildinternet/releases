{
  "name": "releases-api",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply released-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply released-db --remote"
  },
  "dependencies": {
    "hono": "^4.12.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260417.1",
    "wrangler": "^4.83.0"
  }
}
