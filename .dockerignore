# Ignore everything, then allowlist only what the sandbox needs
*

# Project config
!CLAUDE.md
!package.json
!bun.lock
!tsconfig.json

# Application source
!src/

# Agent skills (copied to .claude/skills/ in Dockerfile)
!src/agent/skills/

# Sandbox WebSocket log server
!workers/discovery/src/
