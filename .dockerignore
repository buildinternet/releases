# Ignore everything, then allowlist only what the sandbox needs
*

# Compiled binaries
!dist/released
!dist/released-mcp-browser

# Agent skills (copied to /usr/share/released/skills/ and .claude/skills/ in Dockerfile)
!src/agent/skills/
