---
"@buildinternet/releases-core": minor
"@buildinternet/releases-api-types": minor
---

Add HTTP idempotency primitives from #2238: the `idempotency_records` schema table and the `idempotency_conflict` / `idempotency_in_progress` / `idempotency_unavailable` error codes. api-types is co-bumped per the release convention.
