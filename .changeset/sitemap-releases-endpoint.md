---
"@buildinternet/releases-api-types": patch
---

Add `SitemapReleaseSchema` / `SitemapReleasesPayloadSchema` for the new `GET /v1/sitemap/releases` endpoint (curated, importance-gated release sitemap, #1181 scoped down).
