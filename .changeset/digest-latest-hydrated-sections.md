---
"@buildinternet/releases-api-types": minor
---

Digest detail sections carry an optional `releases` array (the section's cited releases, resolved server-side), and the detail schema also describes the new `GET /v1/collections/:slug/digests/latest` route.
