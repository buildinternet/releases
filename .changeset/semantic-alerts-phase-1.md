---
"@buildinternet/releases-api-types": minor
---

Add semantic alert wire types for `GET/POST/PATCH/DELETE /v1/me/semantic-alerts` and `semanticAlerts` on the notifications settings bootstrap (#2304). Phase 1 stores preferences only.
