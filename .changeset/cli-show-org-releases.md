---
"@buildinternet/releases": minor
---

`releases show <org>` now lists the org's 10 most recent releases inline instead of returning bare metadata, and the output no longer points users at admin-only commands. Also enriches `releases show <release-id>` with the content summary, and updates the product hint to a public command.
