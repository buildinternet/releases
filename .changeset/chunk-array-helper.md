---
"@buildinternet/releases-core": minor
---

Export a shared `chunkArray` helper from `@buildinternet/releases-core/d1-limits` for splitting an array into fixed-size chunks (used to stay under D1's bound-parameter cap).
