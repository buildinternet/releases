# dependencies (bun install)
node_modules

# output
out
dist
*.tgz
*.bun-build

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json
*.log

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
.next
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# SQLite database
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# Local development data
.context
.superpowers
.codex/config.toml
.codex/hooks.json

# Session data
data/sessions/
data/preferences.json

# Git worktrees
.worktrees

# Wrangler local state
.wrangler
.dev.vars

# Vercel
.vercel

# Next.js auto-generated type file (regenerated by dev server and builds)
web/next-env.d.ts

.claude/worktrees/
.agents/skills

# Eval results (generated by bun run eval:*)
tests/evals/results/

# npm publish artifacts (compiled binaries + temp auth)
npm/*/releases
npm/.npmrc

# drizzle-kit generate output — scaffold only, source of truth is workers/api/migrations/
.drizzle-out/
