## v0.44.2

- Manual API updates.
- Internal codegen run.
- Bumped `@types/node` to 20.14.0.
- Format chores across the repo.
- Fixed a regression where `parse()` silently dropped trailing array elements
  when the input exceeded 1000 entries.
