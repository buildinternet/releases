# Bun configuration for the CLI and local tooling.
# Workers-specific runtimes are excluded so Bun never tries to resolve them.
[install.external]
cloudflare = ["cloudflare:workers", "cloudflare:sockets", "cloudflare:email"]
