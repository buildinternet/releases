# Released API URL — defaults to localhost for local development
RELEASED_API_URL=https://api.releases.sh

# API key for authenticated requests (required for production)
# RELEASED_API_KEY=

# Enable dev-only status dashboard (set to "true" to enable)
# RELEASED_DEV_MODE=true
