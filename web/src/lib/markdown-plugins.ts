// Web barrel for the shared changelog markdown plugin stack. The implementation
// lives in `@releases/rendering/markdown-plugins` so the MCP App UI renders
// changelog markdown identically — no drift between web and the app bundle.
export * from "@releases/rendering/markdown-plugins";
