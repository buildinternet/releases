{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "bun install",
  "outputDirectory": ".next",
  "ignoreCommand": "git fetch origin \"$VERCEL_GIT_PREVIOUS_SHA\" --depth=1 -q 2>/dev/null; git diff --quiet \"${VERCEL_GIT_PREVIOUS_SHA:-HEAD^}\" HEAD -- :/web :/packages :/scripts :/bun.lock :/package.json || exit 1"
}
